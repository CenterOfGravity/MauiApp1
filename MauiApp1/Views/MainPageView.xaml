<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             xmlns:local="clr-namespace:MauiApp1.ViewModels">
    <ContentPage.BindingContext>
        <local:MainPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <StackLayout Orientation="Horizontal">
                <Entry Text="{Binding A}"  />

                <Label  Text="{Binding LbLSign}"  VerticalOptions="Center" />

                <Entry Text="{Binding B}" />

                <Label Text="=" VerticalOptions="Center"  />
                <Label  Text="{Binding Result}" VerticalOptions="Center"/>
            </StackLayout>


            <Button Text="Sumar"  Command="{Binding AddBtnCommand}"/>
            <Button Text="Restar" Command="{Binding SubtractBtnCommand}"/>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
